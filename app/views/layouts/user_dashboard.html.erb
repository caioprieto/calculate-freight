<!DOCTYPE html>
<html>
  <head>
    <title>Crie ou logue na sua conta</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "user_dashboard" %>
    <%= stylesheet_link_tag "user_redacoes" %>
    <%= stylesheet_link_tag "user_settings" %>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/js/select2.min.js"></script>
    <!-- Bootstrap 5 Bundle (inclui Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Bootstrap Bundle inclui Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  </head>

  <body>
    <div class="container-dashboard">
      <div class="left-panel">
        <%= render partial: "layouts/shared/left_panel" %>
      </div>
      <div class="right-panel">
        <div class="scrollable-content">
          <div class="container-right-panel">
            <nav class="d-flex align-items-center justify-content-between px-3 py-2">
              <div class="d-flex align-items-center" style="gap: 34px; width: 100%;">
                <%= form_with url: current_page?("/user/redacoes") ? user_redacoes_search_path : user_explorar_path, method: :get, local: true, class: "search-form d-flex align-items-center" do %>
                  <input
                    type="text"
                    name="query"
                    placeholder="Qual <%= current_page?("/user/redacoes") ? "redação" : "curso" %> está procurando?"
                    class="search-input"
                  />
                  <button type="submit" class="search-button">
                    <i class="bi bi-search fs-4"></i>
                  </button>
                <% end %>

                <a href="/user/settings" class="btn d-flex align-items-center">
                  <i class="bi bi-person-circle fs-1 text-dark me-2"></i>
                  <span class="email-text"><%= current_user.nome.upcase %></span>
                </a>
              </div>
            </nav>

            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
