<div class="modulo nested-fields card-modulos" data-new-record="<%= local_assigns[:new_record] ? "true" : "false" %>" data-index="<%= f.index %>">
  <%= f.hidden_field :_destroy, class: "destroy-flag" %>

  <div>
    <%= f.label :titulo, "Título do Módulo", class: "form-label" %>
    <%= f.text_field :titulo, class: "form-control form-control-sm", style: "margin-top: -8px" %>
  </div>

  <div class="mt-1">
    <%= f.label :description, "Descrição", class: "form-label fs-6" %>
    <%= f.text_area :description, class: "form-control form-control-sm", style: "margin-top: -8px", rows: 1 %>
  </div>

  <button type="button" class="btn btn-primary add-aula mt-3">+ Adicionar Aula</button>

  <button type="button" class="btn btn-danger remove-modulo mt-2 mb-2 p-0">
    <i class="bi bi-trash-fill fs-5"></i>
  </button>

  <div class="aulas">
    <%= f.fields_for :aulas do |aula_fields| %>
      <%= render "aulas_nested", f: aula_fields %>
    <% end %>
  </div>
</div>
